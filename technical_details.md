# Meeting Vibe Checker - Technical Details

本ドキュメントでは、Meeting Vibe Checkerのフロントエンドおよびバックエンドの詳細な実装仕様について解説します。

## 1. 全体アーキテクチャ
システムは3つの主要コンポーネントで構成され、Docker Composeによってオーケストレーションされています。

- **Frontend**: ユーザーインターフェースとカメラ制御を担当。
- **Backend API**: 画像解析エンジン（DeepFace）とビジネスロジックを保持。
- **Database**: 解析結果の永続化を担当。

## 2. フロントエンド実装の詳細 (Next.js)

### デザインシステム
- **スタイリング**: Tailwind CSS を使用し、ダークモードを基調としたプレミアムなデザインを実現。
- **コンポーネント**: 
  - `VibeGauge`: Recharts を使用し、感情の分布をリアルタイムに視覚化。
  - `CameraPreview`: ブラウザのカメラストリームを表示。

### カメラ制御と解析フロー
- `getUserMedia` を使用してカメラストリームを取得。
- 3〜5秒間隔で `canvas` にフレームを描画し、Base64形式の画像データとしてバックエンドへ送信。
- 通信には非典型的な効率を重視し、非同期で `fetch` リクエストを実行。

### API クライアント (`lib/api.ts`)
- `USE_MOCK` フラグにより、開発用のモックモードと本番用の実API通信を切り替え可能。
- 統合テスト時には環境変数 `NEXT_PUBLIC_USE_MOCK=false` を設定することで実機連携を確認しました。

## 3. バックエンド実装の詳細 (FastAPI)

### 感情解析エンジン (`app/services/analyzer.py`)
- **DeepFace**: 強力な感情解析ライブラリを使用。
- **解析アルゴリズム**: 顔検出後の表情から 7 種類の感情（happy, angry, sad, etc.）を 0.0〜1.0 のスコアで算出。

### プライバシーとセキュリティ
- **画像破棄**: 受信した画像データはメモリ上（NumPy配列）でのみ保持され、解析完了後に `del` 文とガベージコレクションによって即座にメモリから削除されます。
- **データ保存**: データベースには「誰がいつどの感情だったか」の数値データのみを保存し、画像は一切保存しません。

### 非同期 DB 操作
- **SQLAlchemy 2.0 (Async)**: `asyncpg` ドライバーを使用し、リクエスト処理中に DB I/O による停止が発生しないよう設計されています。

## 4. データベース設計 (PostgreSQL)

### 主要テーブル
- `sessions`: 計測セッション情報（ID, タイトル, 開始時刻, ステータス）。
- `vibe_records`: 解析結果（セッションID, 各感情のスコア, タイムスタンプ）。

### マイグレーション
- Alembic を使用してスキーマを管理。開発中の変更も安全に適用可能です。
