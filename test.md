# Meeting Vibe Checker - Test Documentation

本プロジェクトでは、AI解析の正確性、APIの整合性、およびシステム全体の導通を確保するために、多層的なテストを実施しています。

## 1. バックエンド単体テスト (Backend Unit Tests)

`pytest` を使用して、バックエンドの主要ロジックを検証します。

### 検証内容
- **感情解析ロジック**: 
  - 画像データが正しく解析され、感情スコアが返されるか。
  - 顔が検出されない場合に適切なデフォルト値（Neutral 100%等）が返されるか。
- **API エンドポイント**: 
  - セッションの作成、更新、削除が正常に行えるか。
  - 存在しないセッションIDに対する404エラーなどの例外処理。
- **プライバシー保護**: 
  - 解析処理後にメモリ内に画像データが残っていないことを検証。

### 実行コマンド
```bash
docker compose run --rm -e PYTHONPATH=/app backend pytest tests/ -v
```

## 2. 統合テスト (End-to-End Integration Tests)

フロントエンドとバックエンドを実際に接続し、ユーザーの操作フローに沿って検証します。

### 検証内容
- **疎通確認**: フロントエンドからバックエンドのAPI（`/api/sessions` 等）が正常に呼び出せること。
- **データ永続化**: フロントエンドで作成したセッションが実際に PostgreSQL データベースに保存され、再表示できること。
- **リアルタイム解析フロー**: カメラから送信されたフレームがバックエンドで解析され、その結果がフロントエンドのグラフに正しく反映されること。

### 実行手順
1. `docker compose up --build -d` で環境を起動。
2. ブラウザで `http://localhost:3000` にアクセス。
3. セッションを開始し、カメラの使用を許可（あるいはテスト用モックを使用）。
4. 解析結果が「Emotion Spectrum」カードに反映されることを確認。

## 3. テストの合格基準
- 全ての `pytest` ケースがグリーン（Pass）であること。
- 統合テストにおいて、フロントエンドの「Recent Sessions」リストとデータベースのレコード数が一致すること。
- 画像データがDBやログ、ディスクに一切保存されていないこと。
